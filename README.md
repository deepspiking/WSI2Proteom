# WSI2Proteom

Minimal training/data pipeline for predicting proteome targets from precomputed WSI features.

Current scope:
- build paired slide/aliquot manifest from BRCA artifacts
- build grouped train/val/test splits
- load paired samples for PyTorch training
